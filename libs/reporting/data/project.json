{
  "name": "@cmz/reporting-data",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/reporting/data/src",
  "projectType": "library",
  "tags": ["scope:reporting", "type:data"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit --project libs/reporting/data/tsconfig.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
