{
  "name": "@cmz/reporting-application",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/reporting/application/src",
  "projectType": "library",
  "tags": ["scope:reporting", "type:application"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit --project libs/reporting/application/tsconfig.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
