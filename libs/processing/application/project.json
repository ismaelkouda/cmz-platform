{
  "name": "@cmz/processing-application",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/processing/application/src",
  "projectType": "library",
  "tags": ["scope:processing", "type:application"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit --project libs/processing/application/tsconfig.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx vitest run --config libs/processing/application/vite.config.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
