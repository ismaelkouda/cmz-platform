{
  "name": "@cmz/processing-data",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/processing/data/src",
  "projectType": "library",
  "tags": ["scope:processing", "type:data"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit --project libs/processing/data/tsconfig.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx vitest run --config libs/processing/data/vite.config.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
