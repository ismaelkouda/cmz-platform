/** Style d'affichage du statut privacy-policy — concern UI (hors domaine). */
export enum PrivacyPolicyStatusStyle {
    PUBLISH = 'COMMON.PUBLISH_STYLE',
    UNPUBLISH = 'COMMON.UNPUBLISH_STYLE',
}
