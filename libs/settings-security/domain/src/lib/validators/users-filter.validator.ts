import { UsersFilterContract } from '../contracts/users-filter.contract';

/**
 * Aucun champ requis pour ce filtre — recherche libre + facettes optionnelles.
 */
export function validateUsersFilter(contract: UsersFilterContract): void {
    void contract;
}
