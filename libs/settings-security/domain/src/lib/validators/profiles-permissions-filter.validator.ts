import { ProfilesPermissionsFilterContract } from '../contracts/profiles-permissions-filter.contract';

/**
 * Aucun champ requis pour ce filtre — recherche libre + facettes optionnelles.
 */
export function validateProfilesPermissionsFilter(
    contract: ProfilesPermissionsFilterContract
): void {
    void contract;
}
