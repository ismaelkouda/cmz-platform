import { TeamsFilterContract } from '../contracts/teams-filter.contract';

/** Aucun champ requis pour ce filtre — aucune contrainte structurelle. */
export function validateTeamsFilter(contract: TeamsFilterContract): void {
    void contract;
}
