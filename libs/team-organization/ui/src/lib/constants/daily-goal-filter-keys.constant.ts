/** Wire legacy `daily-goal` : `startDate`/`endDate` seulement, voir `DailyGoalFilterContract`. */
export const DAILY_GOAL_FILTER_KEYS = {
    START_DATE: 'startDate',
    END_DATE: 'endDate',
} as const;
