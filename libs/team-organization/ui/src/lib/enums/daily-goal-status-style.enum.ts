/** Style d'affichage du statut objectif journalier — concern UI (hors domaine), même convention que `AgentsPerformancesStatusStyle`. */
export enum DailyGoalStatusStyle {
    ACTIVE = 'COMMON.ACTIVE_STYLE',
    INACTIVE = 'COMMON.INACTIVE_STYLE',
}
