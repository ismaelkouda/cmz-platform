/**
 * Identifiants seed mock — SSoT pour mock-server + e2e Playwright (T12-6).
 * Pas de secrets réels. Ne pas y coller de credentials staging.
 */
export const MOCK_CREDENTIALS = {
    email: 'admin@cmz.tg',
    password: 'Password123!',
};

export const MOCK_RESET_TOKEN = 'valid-token';
