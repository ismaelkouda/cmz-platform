/**
 * Les sélecteurs conditionnels sont ajoutés seulement avec deux fixtures
 * prouvant les branches true/false. `always` appartient au moteur et ne peut
 * jamais être redéfini ici.
 */
export const FORM_SELECTORS = Object.freeze({});
