{
  "schema_version": "1.0.0",
  "library": "angular-material",
  "platform": "angular",
  "packages": ["@angular/material", "@angular/cdk"],
  "install": {
    "method": "official-schematic",
    "command": {
      "executable": "nx",
      "argv": ["g", "@angular/material:ng-add", "--project", "{{app}}"]
    },
    "notes": "Schematic maintenu par l'équipe Angular, version-aware. L'exécuteur (add-library, lot B) installe d'abord @angular/material + @angular/cdk alignés sur @angular/core, puis lance ce schematic avec `{{app}}` = nom du projet Nx. Il écrit un thème M3 (`mat.theme()`) et la typographie. Les flags exacts et le mécanisme d'animations changent version après version — rien de version-spécifique dans les static_invariants ; la preuve de fonctionnement est runtime_acceptance (harnais lot C)."
  },
  "static_invariants": [
    {
      "id": "m3-theme",
      "description": "un thème M3 est défini dans les styles globaux via mat.theme()",
      "footprint": true,
      "assert": {
        "file": "src/styles.scss",
        "kind": "file-matches",
        "value": "mat\\.theme\\("
      }
    }
  ],
  "runtime_acceptance": [
    {
      "id": "material-component-compiles",
      "description": "dans un workspace candidat autonome : un composant standalone important un composant Material représentatif (ex. MatButton) compile sous ngc --strictTemplates en import standalone (jamais un NgModule agrégateur). Preuve de compilation uniquement — le rendu DOM et le comportement d'animation sont des critères distincts (browser-coexistence + preuve dédiée, à définir).",
      "proof": "compile-component",
      "status": "harness-pending"
    }
  ],
  "coexistence": [
    {
      "with": "tailwind",
      "static_invariants": [
        {
          "id": "component-boundary",
          "description": "les styles globaux documentent la frontière : Material pour les composants, Tailwind pour layout/espacement — le preflight Tailwind ne doit pas réinitialiser les composants Material",
          "assert": {
            "file": "src/styles.scss",
            "kind": "file-matches",
            "value": "[Mm]aterial.*[Tt]ailwind|[Tt]ailwind.*[Mm]aterial"
          }
        }
      ],
      "runtime_acceptance": [
        {
          "id": "material-tailwind-render-together",
          "description": "dans une app candidate isolée : un bouton Material et des utilitaires Tailwind rendus sur la même page ; test navigateur — le bouton garde ses styles Material (hauteur, ripple, états focus) et les utilitaires Tailwind s'appliquent, sans régression du reset",
          "proof": "browser-coexistence",
          "status": "harness-pending"
        }
      ]
    }
  ],
  "guidance": "https://material.angular.dev/guide/theming"
}
